// STRETCHABLE ARRAY

# include <stdio.h>
# include <stdlib.h>

extern int nmax;
//extern int nums[nmax];   // Tunable Array


main(){
	int next, n = 0;
	printf ("nmax:%d\n", nmax);
/*

	//nums = (int *) malloc (sizeof * nums);
	while (1){
		printf ("Enter %dth number", n + 1);
		if (scanf ("%d", nums + n) != 1 )
			break;
		//nums = (int *) realloc (nums, (++n + 1) * sizeof *nums);
	}
	print_nums(n);

}

print_nums(int n){
	int place;

	for (place = 0; place < n; place++)
		printf ("\nyour %dth number = %d", place, nums [place]);
*/
}
